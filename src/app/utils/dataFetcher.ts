import { fetchPaginatedContent as fetchContent } from "@/lib/data/content-fetcher";
import { ContentType } from "@/types/contentType";

// Re-export for backward compatibility
export const fetchPaginatedContent = fetchContent;
